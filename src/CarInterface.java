public interface CarInterface {
    //public void printName(String name);
    void start();
    void move(int speed);

    /*
     interface is an contract that informs the class that uses the interface must
     implement all the classes in the interface

     can not have private classes
     */

}
